import { module } from "qunit";
// import { module, test } from "qunit";
import { setupTest } from "ember-qunit";
// import { render, triggerKeyEvent } from "@ember/test-helpers";
// import hbs from "htmlbars-inline-precompile";

module("Unit | Component | chat phase1", function(hooks) {
  setupTest(hooks);

  // cannot get this test to pass
  // test("should set message on enter", async function(assert) {
  //   const chatPhase1 = this.owner.lookup("component:chat-phase1");
  //
  //   const message = chatPhase1.set("message", "this is a message");
  //   await render(hbs`<ChatPhase1 />`);
  //   await triggerKeyEvent(".chat-phase1 input", "keydown", "13");
  //
  //   assert.equal(chatPhase1.get("message"), message);
  // });
});
